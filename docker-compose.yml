version: '3.8'

services:
  ai-reviewer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/reviews.db:/app/reviews.db
    restart: always
